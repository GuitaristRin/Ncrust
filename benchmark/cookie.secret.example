# Benchmark 登录 cookie 说明（本文件是示例, 真正的 cookie 放在 cookie.secret, 已 gitignore）

Macrobenchmark 每次 connectedCheck 都会卸载重装被测应用, 手动登录会被清掉,
所以基准用"文件注入"方式提供登录态:

1. 复制本文件为 benchmark/cookie.secret
2. 获取你自己的 MUSIC_U（二选一）:
   a. 手机装 app → 我的 → 扫码登录一次 → 执行:
      adb exec-out run-as com.takahashirinta.ncrust cat shared_prefs/ncrust_prefs.xml
      取出 <string name="user_cookie"> 的值;
   b. 桌面浏览器登录 music.163.com → F12 → Application → Cookies →
      music.163.com 域下复制 MUSIC_U 的值（注意值里可能带 ; 分隔的其他项, 只要 MUSIC_U=... 这一段）;
3. 把 cookie.secret 内容写成一行, 形如（不要引号, 不要换行）:
      MUSIC_U=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

测试运行时 LoginCookieInjector 会在每次迭代 setup 阶段用 run-as 把它写入
被测应用 SharedPreferences, 实现"带登录态的真实首页"测量。